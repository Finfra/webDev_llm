# CLI기반 Vibe코딩 Tool이 필요한 이유

## 1. CLI 기반 Vibe코딩 Tool의 필요성
* **빠른 피드백 루프**: 터미널에서 바로 AI 도구 사용 → IDE 전환 없이 빠른 시도·검증 가능
* **자동화 및 반복 작업 최적화**: CLI 환경에 적합한 스크립트화, 자동화 용이
* **로컬/원격 환경 통일성**: 서버 개발 환경에서도 동일한 워크플로우 적용 가능
* **가벼운 환경**: 별도 IDE나 GUI 도구 없이 텍스트 기반 환경에서 바로 활용

## 2. CLI 기반 Vibe코딩 Tool의 장점
* **생산성 향상**
    - 명령어 기반으로 AI의 도움을 즉시 받을 수 있어 작업 속도 향상
    - 코드 작성, 수정, 요약, 분석 등의 다양한 작업 지원
* **개발 환경 일관성 유지**
    - 로컬, 서버, CI/CD 환경 등 어디서나 동일한 방식으로 사용 가능
* **세션 히스토리 관리**
    - CLI 기반으로 대화 이력을 파일로 관리하여 재현성 확보
* **멀티모달 활용**
    - CLI에서도 텍스트 + 이미지 데이터 처리 가능 (ex: `GeminiCli`)
* **커스터마이징 용이**
    - 사용자 정의 명령어, 스크립트 등을 통해 개인화된 워크플로우 구축 가능

# ClaudeCode

## 1. ClaudeCode 소개
* Anthropic에서 개발한 **AI 기반 CLI 코딩 도구**
* 터미널에서 Claude AI에게 직접 코딩 작업을 위임 가능
* 현재 Research Preview 단계

## 2. ClaudeCode 설치 방법
* 설치:
    ```bash
    npm install -g @anthropic-ai/claude
    ```
* 설정:
    ```bash
    claude config
    ```

## 3. ClaudeCode 특징
* **터미널 기반 AI 코딩**: 명령줄에서 코드 생성 및 수정 요청
* **파일 시스템 접근**: 프로젝트 파일 직접 읽기/쓰기 가능
* **컨텍스트 인식**: 전체 프로젝트 구조 이해
* **실시간 협업**: 개발자와 Claude가 실시간으로 코드 작업

## 4. ClaudeCode 사용법
* 예제 명령어:
    ```bash
    claude -p "구구단 코드를 생성하는 node.js앱 만들어줘"
    claude -p "이 버그 수정해줘" --file src/app.js
    ```
* 장점:
    - 터미널 워크플로우에 자연스럽게 통합
    - VS Code 확장 없이 AI 코딩 가능
    - 프로젝트 전체 컨텍스트 활용

* 참고 링크:
    - [공식 사이트](https://claude.ai/code)
    - [Anthropic 블로그](https://www.anthropic.com/news/claude-code)
    - [문서](https://docs.anthropic.com/claude-code)
    - [GitHub](https://github.com/anthropic-ai/claude-code)

# GeminiCli

## 1. GeminiCli 소개
* **Google Gemini AI**를 터미널에서 사용할 수 있는 CLI 도구
* 코딩, 문서 요약, 이미지 분석, 질문 답변 등 다양한 작업을 CLI에서 실행 가능
* GitHub: [google-gemini/gemini-cli](https://github.com/google-gemini/gemini-cli)

## 2. GeminiCli 설치 방법
* **Ubuntu**
    ```bash
    sudo apt install -y npm
    sudo npm install -g @google/gemini-cli
    gemini  # 최초 실행 시 브라우저 로그인 필요
    ```
* **OracleLinux/AmazonLinux**
    ```bash
    sudo dnf install -y npm
    sudo npm install -g @google/gemini-cli
    gemini
    ```
* **임시 실행**
    ```bash
    npx https://github.com/google-gemini/gemini-cli
    ```

* **서버 환경 (브라우저 없이)**
    1. API 키 발급: [Google AI Studio](https://aistudio.google.com/apikey)
    2. 키파일 저장:
        ```bash
        read -p "Key from https://aistudio.google.com/apikey ?" key
        mkdir -p ~/.ssh/_token
        echo $key > ~/.ssh/_token/googleAIStudio.token
        ```
    3. 환경변수 적용:
        ```bash
        export GEMINI_API_KEY="$(cat ~/.ssh/_token/googleAIStudio.token)" >> ~/.bashrc
        source ~/.bashrc
        gemini -p hi
        ```

## 3. GeminiCli 특징
* **터미널 통합**: 명령줄에서 직접 호출
* **파일 처리**: 텍스트, 이미지, 코드 파일 분석 지원
* **스트리밍 응답**: 실시간 답변 출력
* **세션 히스토리**: 대화 기록 관리
* **Batch 처리**: 다수 파일 일괄 처리
* **멀티모달 지원**: 텍스트 + 이미지 입력

## 4. GeminiCli 사용법
* 기본 명령어:
    | 명령어  | 설명             | 
    | ------- | ---------------- |
    | `/help` | 명령어 목록 출력 |
    | `/quit` | 대화 종료        |

* 예시 워크플로우:
    ```bash
    # 프로젝트 초기화
    mkdir my-app && cd my-app

    # GEMINI.md 생성
    vi GEMINI.md

    # .gemini 디렉토리 및 styleguide.md 작성
    mkdir .gemini
    vi .gemini/styleguide.md

    # config.yaml 예시
    vi .gemini/config.yaml
    ```
* 활용 예:
    - 폴더 정리:
        ```
        xx 폴더의 모든 파일을 일목요연하게 정리해줘
        ```
    - 크롤링:
        ```
        https://daum.net에서 축구 관련 기사 검색해서 playwright 기반 크롤링 후 GEMINI.md에 과정 기록
        ```

* 설정 파일:
    - `~/.gemini/GEMINI.md`: 전역 설정
    - `./GEMINI.md`: 프로젝트별 설정
    - `.gemini/styleguide.md`: 코드 작성 규칙
    - `.gemini/config.yaml`: ignore_patterns 등 정의

---

## 실습: CLI 기반 Vibe코딩 환경 구축하기

### 실습 1: ClaudeCode 기본 사용법
* **실습 목표**
    - ClaudeCode 설치 및 기본 명령어 익히기
    - 터미널에서 AI 코딩 도구 활용법 학습
* **실습 단계**
    1. ClaudeCode 설치 (현재 Research Preview 단계)
        ```bash
        npm install -g @anthropic-ai/claude
        claude config  # API 키 설정
        ```
    2. 기본 사용법 실습
        ```bash
        # 간단한 코드 생성 요청
        claude -p "구구단을 출력하는 Python 스크립트 만들어줘"
        
        # 파일 기반 작업
        claude -p "이 파일의 버그를 찾아서 수정해줘" --file app.js
        
        # 프로젝트 전체 컨텍스트 활용
        claude -p "이 프로젝트에 로그인 기능을 추가해줘"
        ```
    3. 결과 분석 및 VS Code 확장과 비교해보기

### 실습 2: GeminiCli 환경 구성 및 활용
* **실습 목표**
    - GeminiCli 설치 및 기본 설정 완료
    - 다양한 유형의 작업을 CLI에서 수행
* **실습 단계**
    1. GeminiCli 설치
        ```bash
        # macOS/Linux
        sudo npm install -g @google/gemini-cli
        
        # 최초 실행 (브라우저 로그인)
        gemini
        ```
    2. API 키 기반 설정 (서버 환경용)
        ```bash
        # API 키 발급: https://aistudio.google.com/apikey
        export GEMINI_API_KEY="your_api_key_here"
        echo 'export GEMINI_API_KEY="your_api_key_here"' >> ~/.bashrc
        source ~/.bashrc
        
        # 테스트
        gemini -p "안녕하세요, CLI에서 첫 테스트입니다"
        ```
    3. 프로젝트 설정 파일 구성
        ```bash
        mkdir my-cli-project && cd my-cli-project
        
        # GEMINI.md 생성
        cat > GEMINI.md << 'EOF'
        # 프로젝트 설명
        이 프로젝트는 CLI 기반 개발 도구 실습용입니다.
        
        ## 주요 목표
        - CLI에서 AI 도구 활용법 학습
        - 효율적인 개발 워크플로우 구축
        
        ## 개발 규칙
        - Python 3.8+ 사용
        - PEP 8 스타일 가이드 준수
        - 함수와 클래스에 docstring 필수
        EOF
        
        # .gemini 디렉토리 및 설정 파일
        mkdir .gemini
        cat > .gemini/styleguide.md << 'EOF'
        # 코딩 스타일 가이드
        
        ## Python
        - 들여쓰기: 4칸 공백
        - 함수명: snake_case
        - 클래스명: PascalCase
        - 상수: UPPER_CASE
        
        ## 주석
        - 복잡한 로직에 설명 주석 추가
        - TODO 항목은 이슈 번호와 함께 기록
        EOF
        
        cat > .gemini/config.yaml << 'EOF'
        ignore_patterns:
          - "**/*.pyc"
          - "**/__pycache__/**"
          - "**/node_modules/**"
          - "**/.git/**"
          - "**/.env"
        EOF
        ```

### 실습 3: 실전 프로젝트 - 간단한 할일 관리 앱 만들기
* **실습 목표**
    - CLI 도구를 활용해 완전한 프로젝트 개발
    - 코드 생성부터 테스트까지 전 과정 실습
* **실습 단계**
    1. 프로젝트 초기화
        ```bash
        mkdir todo-cli-app && cd todo-cli-app
        
        # GeminiCli로 프로젝트 구조 생성 요청
        gemini -p "Python으로 CLI 기반 할일 관리 앱을 만들고 싶습니다. 
        기능: 할일 추가, 삭제, 목록 보기, 완료 표시
        파일 구조와 기본 코드를 생성해주세요."
        ```
    2. 기능별 구현
        ```bash
        # 각 기능을 단계별로 구현
        gemini -p "todo.py에 할일 추가 기능을 구현해주세요. JSON 파일로 데이터 저장"
        gemini -p "할일 목록 보기 기능을 예쁘게 포맷팅해서 추가해주세요"
        gemini -p "할일 완료 상태 토글 기능을 추가해주세요"
        ```
    3. 테스트 및 개선
        ```bash
        # 테스트 코드 생성
        gemini -p "pytest를 사용한 테스트 코드를 생성해주세요"
        
        # 실행 및 테스트
        python todo.py --help
        python todo.py add "GeminiCli 실습 완료하기"
        python todo.py list
        python todo.py complete 1
        python todo.py list
        ```

### 실습 4: CLI 도구 통합 워크플로우
* **실습 목표**
    - 여러 CLI 도구를 조합한 개발 워크플로우 구축
    - 실제 개발 시나리오에서 활용법 학습
* **실습 단계**
    1. 코드 리뷰 자동화
        ```bash
        # GeminiCli로 코드 리뷰
        gemini -p "이 파일의 코드를 리뷰해주세요. 개선점과 버그 가능성을 알려주세요" -f todo.py
        
        # ClaudeCode로 리팩터링 (설치된 경우)
        claude -p "이 코드를 더 깔끔하게 리팩터링해주세요" --file todo.py
        ```
    2. 문서화 자동 생성
        ```bash
        # README.md 생성
        gemini -p "이 프로젝트의 README.md를 생성해주세요. 설치법, 사용법, 예시 포함"
        
        # API 문서 생성
        gemini -p "todo.py의 모든 함수에 대한 API 문서를 마크다운으로 생성해주세요"
        ```
    3. 배포 준비
        ```bash
        # requirements.txt 생성
        gemini -p "이 프로젝트의 requirements.txt 파일을 생성해주세요"
        
        # Docker 설정
        gemini -p "이 Python 앱을 위한 Dockerfile과 docker-compose.yml을 생성해주세요"
        ```

### 실습 5: 성능 비교 및 워크플로우 최적화
* **실습 목표**
    - 다양한 CLI 도구의 특성 이해
    - 상황별 최적 도구 선택 기준 수립
* **실습 단계**
    1. 응답 품질 비교
        ```bash
        # 같은 요청을 다른 도구로 테스트
        echo "동일한 요청을 여러 도구로 테스트해보기:"
        echo "요청: 'Python으로 피보나치 수열 구현'"
        
        # GeminiCli
        gemini -p "Python으로 피보나치 수열을 구현해주세요. 재귀와 반복문 두 가지 방법으로"
        
        # ClaudeCode (설치된 경우)
        claude -p "Python으로 피보나치 수열을 구현해주세요. 재귀와 반복문 두 가지 방법으로"
        ```
    2. 특화 기능 테스트
        ```bash
        # 이미지 분석 (GeminiCli 멀티모달 기능)
        gemini -p "이 이미지를 분석해서 설명해주세요" --image screenshot.png
        
        # 프로젝트 컨텍스트 이해 (ClaudeCode)
        claude -p "이 프로젝트의 전체 구조를 분석하고 개선점을 제안해주세요"
        ```
    3. 워크플로우 문서화
        ```bash
        # 최적 워크플로우 정리
        cat > cli-workflow-guide.md << 'EOF'
        # CLI 기반 AI 개발 워크플로우
        
        ## 도구별 특화 용도
        - **GeminiCli**: 멀티모달 작업, 이미지 분석, 복잡한 추론
        - **ClaudeCode**: 프로젝트 컨텍스트 이해, 코드 리팩터링
        
        ## 단계별 추천 워크플로우
        1. 프로젝트 초기화: GeminiCli로 전체 구조 설계
        2. 코드 구현: ClaudeCode로 파일별 구현
        3. 리뷰 및 개선: 두 도구 모두 활용해 비교 검토
        4. 문서화: GeminiCli로 포괄적 문서 생성
        
        ## 팁
        - 복잡한 요청은 단계별로 나누어 처리
        - 프로젝트 설정 파일(.gemini/*, CLAUDE.md)을 잘 구성
        - 각 도구의 강점을 파악해 적재적소 활용
        EOF
        ```

## 💡 **실습 완료 후 검토사항**
* CLI 도구와 GUI 도구(VS Code 확장 등)의 장단점 비교
* 개인 개발 워크플로우에 적합한 도구 조합 찾기
* 팀 협업시 CLI 도구 활용 방안 고민
* 자동화 가능한 반복 작업 식별 및 스크립트화

